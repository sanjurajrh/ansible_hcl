---
ansible-navigator:
  ansible:
    inventory:
      entries:
      - /home/student/proj_374/inventory
  execution-environment:
    container-engine: podman
    image: hub.lab.example.com/ee-supported-rhel8
    pull:
      policy: missing
  mode: stdout
  playbook-artifact:
    enable: false
